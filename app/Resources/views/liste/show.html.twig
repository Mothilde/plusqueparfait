{% extends 'base.html.twig' %}
{% block body %}
    <div class="navbar_homepage">
        <nav class="navbar navbar-inverse">
            <div class="container-fluid">
                <ul class="nav navbar-nav">
                    <li class="active"> <a href="{{ path('listerepas_homepage') }}">Accueil</a> </li>
                    <li> <a href="{{ path('liste_index') }}">Mes repas</a> </li>
                    <li> <a href="">Mes invités</a> </li>
                    <li> <a href="{{ path('creationplat_index') }}">Mes recettes</a> </li>
                </ul>
                <form class="navbar-form navbar-right inline-form">
                    <div class="form-group">
                        <input type="search" class="input-sm form-control" placeholder="Recherche">
                        <button type="submit" class="btn btn-primary btn-sm"><span class="glyphicon glyphicon-eye-open"></span> Chercher</button>
                    </div>
                </form>
            </div>
        </nav>
    </div>
    <div class="header_menu">
        <img id="iconetoque" src="{{ asset('assets/images/iconetoque.png') }}" alt="iconetoque" />
        <h1>Mon Repas</h1>
    </div>

    <table>
        <tbody>

            <tr>
                <th>Nom repas</th>
                <td>{{ liste.nomRepas }}</td>
            </tr>
            <tr>
                <th>Date</th>
                <td>{% if liste.dateRepas %}{{ liste.dateRepas|date('Y-m-d H:i:s') }}{% endif %}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ liste.descriptionRepas }}</td>
            </tr>
            <tr>
                <th>Invités</th>
                <td>{{ liste.inviteRepas }}</td>
            </tr>
        </tbody>
    </table>
<div class="liste_show">

            <a href="{{ path('liste_index') }}"><button type="button" class="btn btn-success"><span class="glyphicon glyphicon-chevron-left"></span>Retour à la liste</button></a>

            <a href="{{ path('liste_edit', { 'id': liste.id }) }}"><button type="button" class="btn btn-info glyphicon glyphicon-pencil">&nbsp;Editer</button></a>

            <button data-toggle="modal" class="btn btn-danger" data-target="#infos">
                <i class="glyphicon glyphicon-trash"></i>Supprimer</button>
            <div class="modal fade" id="infos">
                <div class="modal-dialog">
                    <div class="modal-content">
                        <div class="modal-header">
                            <button type="button" class="close" data-dismiss="modal">x</button>
                            <h4 class="modal-title">Confirmation</h4>
                        </div>
                        <div class="modal-body">
                            Etes-vous sur de vouloir supprimer cet article ?
                        </div>
                        {{ form_start(delete_form) }}
                        <div class="modal-footer">
                            {{ form_start(delete_form) }}
                            <button type="submit" value="OUI" class=" btn btn-info">OUI</button>
                            {{ form_end(delete_form) }}
                            <button class="btn btn-info" data-dismiss="modal">NON</button>
                        </div>
                    </div>
                </div>
            </div>
    </div>
{% endblock %}

