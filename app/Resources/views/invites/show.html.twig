{% extends 'base.html.twig' %}
{% block body %}
    <div class="navbar_homepage">
        <nav class="navbar navbar-inverse">
            <div class="container-fluid">
                <ul class="nav navbar-nav">
                    <li> <a href="{{ path('listerepas_homepage') }}">Accueil</a> </li>
                    <li> <a href="{{ path('liste_index') }}">Mes repas</a> </li>
                    <li class="active"> <a href="">Mes invités</a> </li>
                    <li> <a href="{{ path('creationplat_index') }}">Mes recettes</a> </li>
                </ul>
                <form class="navbar-form navbar-right inline-form">
                    <div class="form-group">
                        <input type="search" class="input-sm form-control" placeholder="Recherche">
                        <button type="submit" class="btn btn-primary btn-sm"><span class="glyphicon glyphicon-eye-open"></span> Chercher</button>
                    </div>
                </form>
            </div>
        </nav>
    </div>
    <div class="header_menu">
        <img id="iconetoque" src="{{ asset('assets/images/iconetoque.png') }}" alt="iconetoque" />
        <h1>Mes invités </h1>
    </div>
    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ invite.id }}</td>
            </tr>
            <tr>
                <th>Nom invite</th>
                <td>{{ invite.nomInvite }}</td>
            </tr>
        </tbody>
    </table>
    <div class="liste_show">
            <a href="{{ path('invites_index') }}"><button type="button" class="btn btn-info">Retour à la  liste</button></a>
            <a href="{{ path('invites_edit', { 'id': invite.id }) }}"><button type="button" class="btn btn-success glyphicon glyphicon-pencil">&nbsp;Editer</button></a>
            <input type="submit" class="btn btn-danger" value="Supprimer" />
            {{ form_start(delete_form) }}
            {{ form_end(delete_form) }}
    </div>
{% endblock %}
